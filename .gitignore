# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files — NEVER commit real secrets
.env
.env.local
.env.dev
.env.development
.env.production
.env.prod
.env.test
backend/.env
backend/.env.dev
backend/.env.prod
backend/.env.local

# .env.example is intentionally tracked (template, no real secrets)

backend/uploads/
supabase/.temp/

# ── Security — never commit crypto material or scan reports ──────────────────
*.pem
*.key
*.p12
*.pfx
*.crt
*.cert
secrets/
.secrets
.vault-token
terraform.tfstate
terraform.tfstate.backup
*.tfvars

# Security scan reports (generated by CI — not source code)
semgrep.sarif
eslint-security.sarif
eslint-security.json
npm-audit.json
trivy-results.sarif
codeql-results.sarif
